import type { TVector3Limits } from "../../../types/TVector3";

export default {
  x: {
    min: -360,
    max: 360,
  },
  y: {
    min: -360,
    max: 360,
  },
  z: {
    min: -360,
    max: 360,
  },
} as TVector3Limits;
